# BehaveAI / HERDWISE - Python dependencies
#
# NOTE: torch and torchvision are intentionally NOT listed here.
# They are installed by the launcher (Windows_Launcher / Linux_Launcher) based on the
# user's machine (CPU vs the detected NVIDIA CUDA version), NOT pinned from this file.
# Do not add torch/torchvision here or you will override the machine-specific build.

certifi==2026.2.25
charset-normalizer==3.4.4
colorama==0.4.6
contourpy==1.3.3
cycler==0.12.1
decorator==5.2.1
ffmpeg-python==0.2.0
filelock==3.24.3
fonttools==4.61.1
fsspec==2026.2.0
future==1.0.0
idna==3.11
imageio-ffmpeg==0.6.0
ImageIO==2.37.3
Jinja2==3.1.6
joblib==1.5.3
kiwisolver==1.4.9
MarkupSafe==3.0.3
matplotlib==3.10.8
moviepy==2.2.1
mpmath==1.3.0
networkx==3.6.1
numpy==2.4.2
opencv-python==4.13.0.92
packaging==26.0
pillow==11.3.0
polars-runtime-32==1.38.1
polars==1.38.1
proglog==0.1.12
psutil==7.2.2
pyparsing==3.3.2
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
PyYAML==6.0.3
requests==2.32.5
scikit-learn==1.9.0
scipy==1.17.1
# setuptools intentionally left unpinned: pinning ==82.0.0 conflicts with modern torch
# (which requires setuptools<82) and forces pip to downgrade torch to an old CPU build.
six==1.17.0
sympy==1.14.0
tqdm==4.67.3
typing_extensions==4.15.0
ultralytics-thop==2.0.18
ultralytics==8.4.16
# Required by ultralytics' BoT-SORT/ByteTrack trackers (linear assignment in
# ultralytics/trackers/utils/matching.py). Not pulled automatically into the
# venv, so pin it explicitly. Installs cleanly against numpy 2.4.2.
lap==0.5.13
# Model-export dependencies (ONNX + NCNN formats).
# NOTE: we deliberately do NOT use ultralytics[export]: that extra pulls the full
# TensorFlow / TensorFlow.js / Keras stack, which caps numpy<2.2 and conflicts with
# numpy==2.4.2 (ResolutionImpossible), besides being multi-GB. ONNX + NCNN cover the
# usual YOLO deployment exports. (If you ever need TF/TFJS/CoreML export, that needs a
# separate, looser environment.)
onnx
onnxslim
onnxruntime
ncnn
urllib3==2.6.3
# Tracking evaluation only (BehaveAI_evaluate_tracking.py) -- the HOTA reference
# implementation. Not needed by the pipeline itself. Installed from GitHub. For a
# fully reproducible environment, pin a specific commit (append @<commit>). Uses
# removed numpy aliases (np.float), for which the eval module installs a shim.
trackeval @ git+https://github.com/JonathonLuiten/TrackEval.git
# SAHI (Slicing Aided Hyper Inference) — optional small-object detection for the
# primary detectors (sahi_enabled_* in the INI, off by default). Imported lazily,
# so the pipeline runs without it until a project turns tiling on. Pin the exact
# resolved version once installed in BehaveAI.venv (check numpy/opencv compat).
sahi
